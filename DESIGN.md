# DESIGN

## 目的

学習者が文法カテゴリを選び、「概論 → 各論 → 練習問題」を単元ごとに繰り返せるようにする。

## 画面構成

- 1画面に1段階だけを表示する。
- 上部に「概論」「各論」「単元」「練習問題」の親子関係と現在地を表示する。
- 単元数が増えても上部が縦に伸びないよう、「各論」は既定で格納し、見出しに「現在の単元 ／ 全単元数」を出して現在地を保つ。開閉状態は端末に保存する。
- 本文の直後に主要操作を置く。
- 練習問題は1問ずつ表示し、回答後に正誤・根拠を示す。
- 進捗と回答は端末内に保存し、再訪時に続きから再開する。
- 現在地は「文法カテゴリ ＞ 単元 ＞ 段階」の形で示し、端末への自動保存も明記する。
- 結果画面では得点、誤答、次の学習、各論・概論へ戻る方法を示す。
- 「戻る」は直前の学習状態へ戻し、ボタン名に戻り先を記す。
- 構成リストの各項目（概論・各単元の各論・練習問題）はボタンとし、クリックで直接その段階へ移動できる。未着手・途中でも移動を制限しない。
- 練習問題へ直接移動した場合は、未回答の最初の問題から再開する。全問回答済みなら結果画面を表示する。
- 構成リストは、各単元の実際の回答実績（既論を読んだか、練習問題の得点）をもとに「済み」表示を行う。到達順ではなく実績で判定する。
- 結果画面の操作は「学び直す系（各論を読み直す・もう一度解く）」と「前へ進む決定系（誤答の解き直し・次の単元／概論へ）」の2群に分け、決定系は常に隣接させて折り返す。主CTAは決定系の中で1つ。
- 最終単元の結果画面には、全単元の合計得点、要復習の単元、未実施の単元をまとめた総括を追加する。全単元がマスター済みなら修了テストへ誘導する文言を添える。

## 修了テスト

- コースの構成リストの最後に「修了テスト」を1段追加する（`stagesFor` が付与）。概論・各論・練習問題とは独立した段で、コースをまたいで共有しない。
- 解放条件は「そのコースの全単元がマスター済み（練習問題が全問正解）」。マスターは現在の回答（`answers`）で判定し、到達順・誤答の解き直し履歴は問わない。
- 未解放の間は、あと何単元でどの単元の得点が足りないかを一覧表示する。ロック理由を隠さない。
- 出題はそのコースの全問からランダムな順で1回だけシャッフルする。合格ラインは正答率80%（`Math.ceil(total * 0.8)`）。
- 結果は `state.finalChecks[courseId]` に `bestScore` `lastScore` `cleared` `bestTotal` を保存する。一度CLEARすれば以後も保持するが、単元・問題の増減で合計問題数が変わったら`cleared`を無効化し、挑戦し直しを促す。
- 修了テストの解答は `state.answers`（各論の練習問題の記録）にも `state.review`（下記の復習記録）にも一切書き込まない。単元練習の進捗や間隔反復とは独立した「認定テスト」として扱う。
- 構成リストの「修了テスト」項目はCLEAR済みなら済み表示にする。他の項目と同様、未解放でもクリックでき、ロック画面が理由を説明する。

## 今日の復習（間隔反復）

- 対象は単元ではなく**問題単位**。各問題は content.js の `id` で識別する。単元を増やす・問題を追加するときは、その単元の他の問題と衝突しない明示的な `id`（例 `<lessonId>-qN`）を付ける。一度公開した `id` は変更しない。
- 間隔は Leitner 式で `1 → 3 → 7 → 14日`。正解のたびに次の段へ進み、不正解で最初の段（即再出題）に戻る。
- 復習期限の判定は**その日の0時境界**で行う（回答時刻からの厳密な経過時間ではない）。日をまたげば「期限到来」として扱う。
- 復習の記録は、通常の練習問題に解答した瞬間（初回・retry・retry-wrongを問わない）と、復習セッション内での解答の両方で更新する。修了テストの解答では更新しない。
- 復習の対象は「練習問題を全問回答済みの単元」の問題に限る。未着手・途中の単元の問題は対象にならない。コースをまたいで1つのプールに集約する（コースごとに分離しない）。
- 1回のセッションは最大10問。誤答回数（`wrongCount`）が多い問題を優先して出題する（シャッフル後に安定ソート）。
- ヘッダーに「今日の復習：n問」のバナーを常設し、対象が0件のときは表示しない。復習セッション中は非表示にする。
- 復習セッションは `state.stage`（現在のコース・単元位置）を変更しない別レイヤーとして描画する。開始前にいた画面はセッション終了後にそのまま復元する。

## 保存データ

- 保存キー（`englishGrammarLearning.v3`）はこの機能追加で変更しない。`review` `reviewSession` `finalChecks` `finalRun` は新規の追加フィールドとして扱い、既存の保存データはそのまま読み込める。

## 生徒別進捗（クラウド同期）

- 共有URL（`?s=<id>&t=<token>`）があり `config.json` が揃うときだけ有効にする。無ければ完全にLocalStorageのみの匿名動作のままにする（`vendor/harness/cloud.js` は無回帰を前提に設計されている）。
- クラウドから届いた進捗は、そのまま `state` に差し替えない。`defaultState` で欠損フィールドを埋めたうえで、通常のロード直後と同じ正規化（`clampStage` → `backfillVisitedLessons` → `sanitizePersistedSessions` → `syncContentVersions`）を通す。これを省くと、今回追加した `review` / `finalChecks` 等を持たない古いクラウド保存データを読み込んだときに未定義参照でクラッシュする。
- クラウドに保存履歴が無い（新規発行の生徒URL）場合、`app_load_progress` は空オブジェクトを返す。この場合は端末側の状態をそのまま保持し、`state` を差し替えない。差し替えてしまうと、匿名時代に端末へ貯めていた進捗を空状態で上書きしてしまう。クラウドへの反映は次の `saveState()`（＝生徒の次の操作）を待つ。
- クラウド保存のトリガーは `saveState()` の1箇所にまとめる。呼び出し側（各アクションハンドラ）はクラウドの有無を意識しない。
- 進捗保存の表示（ヘッダーの `#save-status`）は、共有URLで有効な間は harness の `onStatus` が渡す文言（保存中・保存済み・失敗）にその場で置き換える。共有URLが無い匿名時は「端末に自動保存済み」のまま変更しない。
- 端末内のLocalStorageは生徒ごとに分離しない（生徒プロファイル切替UIは持たない）。同じ端末を複数の生徒で使う場合は、各生徒の共有URLでアクセスする運用を前提にする。

## 教材データ

- 教材本文と問題は `content.js`、画面遷移と採点は `app.js` に分ける。
- 教材は `curriculum.courses` のカテゴリごとに「ID・名称・概論・単元」を持つ。
- 単元を増やすときは、対象カテゴリの `lessons` に「ID・名称・本文・問題」を1件追加する。
- 各単元は「各論」の下に置き、その単元の直下へ「練習問題」を自動的に追加する。
- 単元IDは保存済み回答の識別に使うため、公開後は変更しない。
- カテゴリIDも保存済みの選択状態に使うため、公開後は変更しない。
- 各論本文に複数の`<h3>`相当の節がある場合は、`<details class="section"><summary>節見出し</summary>…</details>`で折りたたむ。最初の節は`open`にし、以降は既定で閉じる。単一の補足節だけがある場合は、その節のみを閉じた状態で追加する。

## 視覚ルール

- 背景は淡い灰青、本文面は白、本文色は濃紺とする。
- 青は主要操作、緑は正解、赤は不正解に使い、記号と文章も併記する。
- 本文幅は720pxを上限とし、行長を抑える。
- 画面は1列とし、`min(100% - 32px, 720px)` の共通コンテナを使う。
- ヘッダーと本文の左右余白は、521px以上で48px、520px以下で20pxにそろえる。
- 見出しは短く構造を示し、本文は16px・行間1.75・おおむね30〜40emを保つ。
- 操作領域は44px以上。キーボードのフォーカスを明示する。
- 主CTAは1画面に1つとし、補助操作は白地の副ボタンにする。
- 画面幅が狭い場合は、全体構成を「学習の構成を見る」に折りたたみ、操作ボタンを縦に並べる。
- 折りたたみは `<details>` の開閉に委ね、閉じた `<details>` の中身をCSSで表示させない（現行ブラウザでは無効で、要素が開けなくなる）。
- 画面更新時は本文へフォーカスを移しても、利用者のスクロール位置を勝手に変えない。
