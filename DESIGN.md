# DESIGN

## 目的

学習者が文法カテゴリを選び、「概論 → 各論 → 練習問題」を単元ごとに繰り返せるようにする。姉妹アプリ（`kobun-vocab-learning`）と同じ画面構成・配色・形状・操作感を採用し、初見で同じ製品群と分かる見た目にする。学習構造（カテゴリ→概論→各論→練習→修了テスト）は姉妹アプリの単語学習フローとは異なるため、意味的に対応する要素へ置き換える（例：単語一覧→単元一覧、暗記カード→各論カード）。

## 画面構成

- ホーム（`#homePanel`）とセッション（`#sessionPanel`）の2面構成にする。両者は同時に表示しない（`.hide` で排他制御）。
- 内容レールは `min(920px, 100% - 32px)` を上限とする1カラム（`.wrap`）。サイドバーは持たない。広い画面でも行長そのものは広げず、左右の余白に幅を使う。
- ヘッダー（`.top`）はアプリ名・現在地（パンくず、`#current-path`）・保存状況（`#save-status`）の要約表示に絞る。画面高760px以下のセッション中は`.top`ごと非表示にし、セッション見出しの現在地表示に委ねる。
- ホームは次の順で表示する。
  1. アプリ見出し（`.top` 内、常設）
  2. カテゴリ選択（`.setPicker` 相当のselect、表示ラベル「文法カテゴリ」）
  3. 今回の学習カード（`.card`）：label「今回の学習」／カテゴリ名／カテゴリ概要（lead）
  4. 「まずはここから」の主CTA（`.recommend`、既存 `overviewActionFor()` を利用）
  5. 4指標（`.stats`）：完了単元／要復習単元／マスター済み／修了テストBEST
  6. 今日の復習（間隔復習カード、`.card.meaningMission` 相当）
  7. 単元一覧（`.card`、各行がボタンで概論・各論・練習結果・修了テストへ直接移動できる）
  8. その他（`<details class="card utility">`）
- セッションは次の順で表示する。
  1. セッション見出し（`.sessionHead`）：現在地と問題数のlabel／現在の段階名（h2）／保存状態／ghost「一覧へ戻る」
  2. ステップバー（`.stepBar`）：`概論 / 各論 / 練習 / 修了` を基本とし、現在段階を`active`、完了段階を`cleared`で示す
  3. 各論カード（`.flashCard`相当）または問題カード（`.quiz`）
  4. 戻る・次への操作（`.actions`）
- カテゴリ選択はホームにのみ置く（セッション中は選べない）。切替時はそのカテゴリを最後に離れた位置（ホームから切り替えるため通常は概論）へ戻る。リロード時は、そのとき保存されていた段階・問題位置をそのまま復元する（既存の `coursePositions` ロジックを維持）。
- 単元一覧の各行は番号・単元名・状態文言を持ち、状態を色だけに依存させない。状態は「未着手／各論確認済み／練習途中 n/3／要復習 n/3／全問正解」の5区分。
- 概論の主操作は進捗から導出し、未完了単元、要復習単元、修了テストの順で次の学習先を示す（既存 `overviewActionFor()` を維持）。
- 結果画面の操作は「学び直す系（各論を読み直す・もう一度解く）」と「前へ進む決定系（誤答の解き直し・次の単元／概論へ）」の2群に分け、決定系は常に隣接させて折り返す。主CTAは決定系の中で1つ。画面幅が狭く縦積みになる場合も、決定系グループ内で主CTAが先頭に来る順序にする。
- 最終単元の結果画面には、全単元の合計得点、要復習の単元、未実施の単元をまとめた総括を追加する。

### レスポンシブ

- 640px以下：`.stats` を2列、間隔復習の6セルを3列×2段にする。
- 320px以上で横スクロールが発生しない。ステップバーは必要なら横スクロールできるが、ページ自体ははみ出さない。
- 画面高760px以下のセッション中は共通ヘッダー（`.top`）を隠す。

## 修了テスト

- コースの単元一覧の最後に「修了テスト」を1段追加する（`stagesFor` が付与）。概論・各論・練習問題とは独立した段で、コースをまたいで共有しない。
- 解放条件は「そのコースの全単元がマスター済み（練習問題が全問正解）」。マスターは現在の回答（`answers`）で判定し、到達順・誤答の解き直し履歴は問わない。
- 未解放の間は、あと何単元でどの単元の得点が足りないかを一覧表示する。ロック理由を隠さない。
- 出題はそのコースの全問からランダムな順で1回だけシャッフルする。合格ラインは正答率80%（`Math.ceil(total * 0.8)`）。
- 結果は `state.finalChecks[courseId]` に `bestScore` `lastScore` `cleared` `bestTotal` を保存する。一度CLEARすれば以後も保持するが、単元・問題の増減で合計問題数が変わったら`cleared`を無効化し、挑戦し直しを促す。
- 修了テストの解答は `state.answers`（各論の練習問題の記録）にも `state.review`（下記の復習記録）にも一切書き込まない。単元練習の進捗や間隔反復とは独立した「認定テスト」として扱う。
- 単元一覧の「修了テスト」行はCLEAR済みなら済み表示にする。他の項目と同様、未解放でもクリックでき、ロック画面が理由を説明する。

## 今日の復習（間隔反復）

- 対象は単元ではなく**問題単位**。各問題は content.js の `id` で識別する。単元を増やす・問題を追加するときは、その単元の他の問題と衝突しない明示的な `id`（例 `<lessonId>-qN`）を付ける。一度公開した `id` は変更しない。
- 間隔は Leitner 式で `1 → 3 → 7 → 14日`。正解のたびに次の段へ進み、不正解で最初の段（即再出題）に戻る。
- 復習期限の判定は**その日の0時境界**で行う（回答時刻からの厳密な経過時間ではない）。日をまたげば「期限到来」として扱う。
- 復習の記録は、通常の練習問題に解答した瞬間（初回・retry・retry-wrongを問わない）と、復習セッション内での解答の両方で更新する。修了テストの解答では更新しない。
- 復習の対象は「練習問題を全問回答済みの単元」の問題に限る。未着手・途中の単元の問題は対象にならない。コースをまたいで1つのプールに集約する（コースごとに分離しない）。
- 1回のセッションは最大10問。誤答回数（`wrongCount`）が多い問題を優先して出題する（シャッフル後に安定ソート）。
- ホームに間隔復習カードを表示し、**復習対象（練習問題を全問回答済みの単元の問題）が0件のときはカード全体を表示しない**。復習セッション中も非表示にする。カードは2段構成にする。①「今日の復習：n問」＋開始ボタンの行は、今日やる分（未実施・要再確認・期限到来の合計）が0件のときだけこの行だけを隠す（対象がある限りカード自体は表示し続ける）。②「状態・復習間隔別内訳」の6セル（未実施・要再確認・1日後・3日後・7日後・14日後、LEITNER_LADDERの各間隔から生成）で件数を出す。今日やる分が0件でも表示して進捗の見通しを確認できるようにする。
- 通常学習の途中（`coursePositions` に未完了の位置）がある場合、復習カードのCTAはsecondaryへ落とし、「先に通常学習を再開するのがおすすめ」と表示する。
- 復習セッションは `state.stage`（現在のコース・単元位置）を変更しない別レイヤーとして描画する。開始前にいた画面はセッション終了後にそのまま復元する。
- セッション完了画面は、セッション後もなお期限到来の問題が残っていれば残数を示し「続けて復習する」で次のセッション（最大10問）へ直接進めるようにする。残数が0なら「今日の復習はすべて終わりました」に切り替え、進行ボタンは「学習に戻る」のみにする。

## 保存データ

- 保存キー（`englishGrammarLearning.v3`）は変更しない。`review` `reviewSession` `finalChecks` `finalRun` `coursePositions` は既存の追加フィールドのまま扱い、既存の保存データはそのまま読み込める。
- `coursePositions` はカテゴリごとの `{ stage, question }` を保持する追加フィールド。欠損時はトップレベルの現在位置から補完する。

## 生徒別進捗（クラウド同期）

- 共有URL（`?s=<id>&t=<token>`）があり `config.json` が揃うときだけ有効にする。無ければ完全にLocalStorageのみの匿名動作のままにする（`vendor/harness/cloud.js` は無回帰を前提に設計されている）。
- クラウドから届いた進捗は、そのまま `state` に差し替えない。`defaultState` で欠損フィールドを埋めたうえで、通常のロード直後と同じ正規化（`clampStage` → `backfillVisitedLessons` → `sanitizePersistedSessions` → `syncContentVersions`）を通す。
- クラウドに保存履歴が無い（新規発行の生徒URL）場合、`app_load_progress` は空オブジェクトを返す。この場合は端末側の状態をそのまま保持し、`state` を差し替えない。クラウドへの反映は次の `saveState()`（＝生徒の次の操作）を待つ。
- ただし「端末側を保持」してよいのは、その端末に残っている進捗が今アクセスしている生徒自身のものである場合に限る。クラウド同期が有効になった生徒IDを `<storageKey>.cloudStudent` に記録し、空クラウド＋別生徒IDのスタンプが残っている場合は端末側を保持せず、通常のロード直後と同じ正規化（版チェックを含む）を通した既定状態から始める。
- 共有URLの認証（`app_auth_student`）は通信失敗時に匿名扱いへフォールバックする（`cloud.js` の設計）。
- クラウド保存のトリガーは `saveState()` の1箇所にまとめる。呼び出し側（各アクションハンドラ）はクラウドの有無を意識しない。
- 進捗保存の表示（`#save-status`）は、共有URLで有効な間は harness の `onStatus` が渡す文言（保存中・保存済み・失敗）にその場で置き換える。共有URLが無い匿名時は「端末に自動保存済み」のまま変更しない。
- 端末内のLocalStorageは生徒ごとに分離しない。同じ端末を複数の生徒で使う場合は、各生徒の共有URLでアクセスする運用を前提にする。

## 教材データ

- 教材本文と問題は `content.js`、画面遷移と採点は `app.js` に分ける。
- 教材は `curriculum.courses` のカテゴリごとに「ID・名称・概論・単元」を持つ。
- 単元を増やすときは、対象カテゴリの `lessons` に「ID・名称・本文・問題」を1件追加する。
- 各単元は「各論」の下に置き、その単元の直下へ「練習問題」を自動的に追加する。
- 単元IDは保存済み回答の識別に使うため、公開後は変更しない。カテゴリIDも保存済みの選択状態に使うため、公開後は変更しない。
- 各論本文に複数の`<h3>`相当の節がある場合は、`<details class="section"><summary>節見出し</summary>…</details>`で折りたたむ。最初の節は`open`にし、以降は既定で閉じる。単一の補足節だけがある場合は、その節のみを閉じた状態で追加する。

## 色（役割トークン）

実装は `styles.css` の `--color-*` を正本とする。`kobun-vocab-learning/static/styles.css` と同一の値を採用する。

| 役割トークン | 値 | 用途 |
| --- | --- | --- |
| `--color-canvas` | `#faf9f5` | ページ背景 |
| `--color-surface-primary` | `#efe9de` | カード・各論カード・問題面 |
| `--color-surface-subtle` | `#f5f0e8` | 主CTA面、間隔復習の内訳セル背景 |
| `--color-ink-primary` | `#141413` | 本文・見出し・主要ボタン地色 |
| `--color-ink-secondary` | `#615c54` | 補助文字・ラベル・ヒント |
| `--color-border` | `#ddd5c8` | 罫線・区切り |
| `--color-control-border` | `#8a8478` | select・ghost button・選択肢など操作要素の通常時境界 |
| `--color-accent` | `#a9583e` | 要復習・間隔復習の強調 |
| `--color-accent-strong` | `#8a4732` | accentのhover/active |
| `--color-success` | `#136b31` | 正解のみ |
| `--color-danger` | `#b42318` | 不正解・エラーのみ |

旧トークン（`--parchment` `--paper` `--ink` `--muted` `--clay` `--ok` `--ng` 等）は互換エイリアスとして残すが、値は上記の役割トークンを指す（`--paper` → `--color-surface-primary` など）。移行が完了したクラスから旧トークン参照を削除し、エイリアス自体もTask 8で不要になったものを整理する。

## タイポグラフィ尺度

| 名称 | サイズ | 書体 | 用途 |
| --- | --- | --- | --- |
| display | 48px | Georgia serif | 完了画面のスコア |
| page title | clamp(32px, 6vw, 52px) | Georgia/Yu Mincho | `h1` |
| section title | clamp(25px, 4vw, 34px) | Georgia/Yu Mincho | `h2`（概論・各論・結果見出し） |
| question title | clamp(26px, 5vw, 38px) | Georgia/Yu Mincho | 練習問題・修了テストの問題文 |
| body | 16px / line-height 1.7 | Segoe UI系 | 本文・選択肢・解説 |
| helper | 13-14px | Segoe UI系 | ヒント・補助文・保存状態 |
| label | 12px, 700, uppercase | Segoe UI系 | セクションラベルのみ（日本語ラベルは非uppercase） |
| numeric | `font-variant-numeric: tabular-nums` | — | 統計値・スコア・進捗番号 |

## 形状ルール

- コンテナ（`.card`, `.flashCard`, `.quiz`, `.doneBanner` など）: `border-radius: 12px`
- 操作要素（ボタン、select、選択肢など）: `border-radius: 8px`
- 番号のみ円形（選択肢番号・単元一覧の番号）: `border-radius: 999px`
- 例外: ホームの「その他」は主導線から一段下げるため`border-radius: 0`（罫線区切りのみ）とする。
- 影は使わず、背景色と細い罫線で階層を作る。

## Motion

Kinetics（参考: https://kinetics.colorion.co/）のAccordion Spring・Icon Morph Swap・Step Progress・Success Check・Submit States・Card Resizeを参考に、既存のhover/press/feedback規則をCSSと少数のJS helperで拡張する。姉妹アプリ（`kobun-vocab-learning`）と共有するhover/feedbackの数値・用途は変えない。

### token

- hover: 180ms, `background-color`/`border-color`/`color`の`transition`（既存、姉妹アプリと共通）
- press（`:active`）: 80ms, `transform: scale(.98)`相当の短い沈み込み
- release（pressの戻り・hover復帰）: 180ms, `--ease-spring-soft`を使う
- feedback reveal（正誤表示、`.feedback`）: 200ms, `opacity` + 8px程度の`translateY`（既存、姉妹アプリと共通）
- progress（Step Progressのactive移動・cleared化の短いsettle）: 280ms
- accordion（各論`<details class="section">`の矢印・本文の開閉）: 260ms
- completion（修了テスト解放の一度きりの強調）: 420ms
- `--ease-spring-soft: cubic-bezier(.34, 1.2, .64, 1)`。押し込み（press）には使わず、戻り・展開・強調（release/progress/accordion/completion）にだけ使う。
- `prefers-reduced-motion: reduce` では上記すべての`transition`/`animation`を`0ms`にし、即時表示にする。
- 使用プロパティは `transform`・`opacity`・色変化のみ。レイアウトを揺らす`width`/`height`/`margin`のアニメーションは行わない。固定`max-height`によるアコーディオンの高さアニメーションも行わない（長い節が切れるため）。

### Accordion（各論の`<details class="section">`）

- `<summary>`内の矢印（疑似要素またはspan）だけを`transform: rotate()`で回転させ、開閉を示す。
- 本文は`opacity` + `translateY(4〜6px)`で表示する。高さは`<details>`のネイティブな挙動に任せ、固定`max-height`は使わない。
- `<details>`のネイティブな意味・キーボード操作（Space/Enter開閉）を変えない。開閉途中の再クリックでも最終的な`open`属性と表示が一致する。

### 修了テスト解放

- 単元一覧の「修了テスト」行の一時的な強調（unlock表現）は、その操作の直前直後で状態が「未解放→解放」に変わったときだけ再生する。永続的な「演出済み」フラグは保存データに追加せず、現在操作内の前後比較だけで判定する。
- リロード、カテゴリ再訪、すでに解放済みの場合は再演しない。

### 原則

- 1操作1主役：1つの操作に対して同時に動く要素は1つの主役に絞る。複数要素が同時に主張する演出は避ける。
- 完了待ち禁止：アニメーションの完了を待たないと次の操作ができない実装をしない（`animationend`を操作の可否に使わない）。
- 動きだけに意味を持たせない：状態の違いは文言・記号・色などモーション以外の手がかりでも伝える。モーションを状態の唯一の手がかりにしない。

## 状態マトリクス

このアプリは `content.js`/`app.js` を同期スクリプトタグで読み込み、初期表示にfetchを伴わない（`kobun-vocab-learning` はマニフェスト・データセットをfetchする点が異なる）。そのため「loading」状態の実際のギャップはスクリプト読込直後のごく短い時間に限られるが、視覚的な一貫性のため同型の静的skeletonを保持する。

| 画面/要素 | loading | empty | error | 正解 | 不正解 | 完了 |
| --- | --- | --- | --- | --- | --- | --- |
| ホーム初回表示 | 静的skeleton（`aria-busy`、JS初期化後に解除） | — | — | — | — | — |
| 今日の復習カード | — | 対象0件はカード非表示 | — | — | — | — |
| クラウド同期 | — | — | インライン`#save-status`にエラー文言（`alert()`不使用） | — | — | — |
| 練習問題／今日の復習／修了テスト | — | — | — | 選択肢に○＋緑罫線＋`.feedback.ok` | 選択したものに×＋赤罫線、正答も同時表示＋`.feedback.ng` | — |
| 単元結果／修了テスト結果／復習セッション完了 | — | 要復習0件は「全問正解しました」相当の文言 | — | — | — | `.doneBanner`（正誤色は強調面のみ、反転しない） |
| 修了テスト | — | 未解放時はロック理由と不足単元を一覧表示 | — | — | — | 未解放→解放の変化時だけ一度`is-unlocking`相当を再生（`completion` 420ms）。リロード・再訪・解放済みでは再演しない |
| 各論`<details class="section">` | — | — | — | — | — | 開閉のたびに矢印回転＋本文opacity/translateYを再生（`accordion` 260ms） |
| ステップバー（`.step`） | — | — | — | — | — | active移動・cleared化のときだけ対象stepのみsettle（`progress` 280ms）。初期描画・リロードでは全stepを再演しない |
| 保存状態（`#save-status`） | syncing中はloader表示 | — | 保存失敗は静的`!`（アニメーションなし） | — | — | saved化の瞬間だけcheckを一度settle。同文言の再設定はしない（`aria-live=polite`） |

## グリッド規則

- 内容レール: `min(920px, 100% - 32px)`。スマートフォンは左右10px（`100% - 20px`）。
- カード内側: デスクトップ24px、スマートフォン18px。
- 基本グリッド間隔: 12px。ステップバー間隔: 8px。
- ブレークポイント: 640px以下で4指標・間隔復習を2列/3列にする。
- 説明文・解説の読み幅: 最大44rem。問題文・選択肢は内容に応じて全幅を使う。
- 画面高760px以下の学習中は共通ヘッダーを隠し、学習見出しと操作を同じ画面内に収める。

## コンポーネント役割

| 役割 | 見た目 |
| --- | --- |
| 主要操作 | 墨色の塗りボタン、角丸8px、最小高さ48px |
| 復習操作 | 焦茶の塗りボタン、または焦茶のアウトライン |
| 補助操作 | 罫線のみのゴーストボタン |
| 状態 | 文言・記号・左罫線を併用し、色だけに依存しない |

### 4択問題の操作

- デスクトップ（641px以上）の4択問題では、回答後の主要な次操作（「次の問題」）を画面下部に固定表示し、スクロールなしで押せるようにする。
- スマートフォン（640px以下）では固定表示に戻さず、解説の下へインライン表示する。
- キーボード操作として、4択は `1`〜`4`、回答後の次操作は `Enter` でも実行できる。
- キーボード操作は、IME変換中・フォーム入力中・修飾キー（Ctrl/Alt/Meta/Shift）併用時には発火させない。
- ショートカットが機能するには、回答後のフォーカスがボタン以外（問題見出し・feedback、いずれも`tabindex="-1"`）にあることが前提となる。ボタンにフォーカスが残ると`closest("button")`のガードでショートカットが無効化される。

## 実装ノート

- 影・グラデーション・GSAP等の新規依存は追加しない。
- 画面更新時は本文へフォーカスを移しても、利用者のスクロール位置を勝手に変えない（`preventScroll: true`）。
- 折りたたみは `<details>` の開閉に委ね、閉じた `<details>` の中身をCSSで表示させない実装をしない。
