# DESIGN

## 目的

学習者が文法カテゴリを選び、「概論 → 各論 → 練習問題」を単元ごとに繰り返せるようにする。

## 画面構成

- 1画面に1段階だけを表示する。
- 画面はヘッダー（上部）とサイドバー＋本文の2カラムで構成する。サイドバーには「今日の復習」バナー・文法カテゴリ選択・構成リスト（概論／各論・練習問題／修了テスト）を上から順に置き、画面幅1000px以上では`position: sticky`で常時表示する。ヘッダーは「ブランド名・現在地（パンくず）・保存状況」の要約表示に絞り、縦に伸びる要素を置かない。何段階目でも高さがほぼ一定になることを優先する。
- サイドバーの構成リストは単元ごとに折りたたみグループとし、見出しに単元名と実績要約（未着手／各論：済／練習問題n問中m問正解／全問正解）を出す。現在地を含む単元グループだけを既定で展開し、他は見出しのみに畳む。開閉状態は現在地（`state.stage`）から都度導出し、端末には保存しない。
- 概論（各コースのホーム画面）は本文冒頭に、そのコースの学習状況ダッシュボード（完了単元数／マスター単元数／修了テストの状態、いずれも分母付き）をカード3枚で示す。全体像を先に示し、単元単位の詳細な移動はサイドバーの構成リストに委ねて重複させない。
- 本文の直後に主要操作を置く。
- 本文カード（`#content`）の実測幅は680pxを上限に固定する。シェル自体は画面幅に応じて広がるが、広い画面で使うのはサイドバーの常時可視性と余白であり、行長ではない。
- 練習問題は1問ずつ表示し、回答後に正誤・根拠を示す。
- 進捗と回答は端末内に保存し、再訪時に続きから再開する。
- 現在地は「文法カテゴリ ＞ 単元 ＞ 段階」の形で示し、端末への自動保存も明記する。
- 結果画面では得点、誤答、次の学習、各論・概論へ戻る方法を示す。
- 「戻る」は直前の学習状態へ戻し、ボタン名に戻り先を記す。
- 構成リストの各項目（概論・各単元の各論・練習問題・修了テスト）はボタンとし、クリックで直接その段階へ移動できる。未着手・途中でも移動を制限しない。折りたたまれた単元グループは見出しをクリックして展開してから移動する。
- 練習問題へ直接移動した場合は、未回答の最初の問題から再開する。全問回答済みなら結果画面を表示する。
- 構成リストは、各単元の実際の回答実績（各論を読んだか、練習問題の得点）をもとに「済み」表示を行う。到達順ではなく実績で判定する。修了テストの行は`finalStatusFor`の判定（未解放／挑戦可能／CLEAR）をそのままバッジ表示する。
- 結果画面の操作は「学び直す系（各論を読み直す・もう一度解く）」と「前へ進む決定系（誤答の解き直し・次の単元／概論へ）」の2群に分け、決定系は常に隣接させて折り返す。主CTAは決定系の中で1つ。画面幅が狭く縦積みになる場合も、決定系グループ内で主CTAが先頭（最上部）に来る順序にする。
- 最終単元の結果画面には、全単元の合計得点、要復習の単元、未実施の単元をまとめた総括を追加する。全単元がマスター済みなら修了テストへ誘導する文言を添える。

### レスポンシブ

- 1000px以上：サイドバーは`position: sticky`で本文の左に常時表示する。
- 640〜1000px：サイドバーは`position: static`になり本文の上に積むが、常時展開のままで折りたたみトグルは出さない。
- 640px以下：「今日の復習」バナーと文法カテゴリ選択は常時表示のまま、構成リストだけを「学習の構成を見る」の1本の`<details>`に格納する（既定は閉）。開閉は画面幅の変化に応じてJS（`syncProgressDetails`）が強制し、手動での開閉状態は保存しない。

## 修了テスト

- コースの構成リストの最後に「修了テスト」を1段追加する（`stagesFor` が付与）。概論・各論・練習問題とは独立した段で、コースをまたいで共有しない。
- 解放条件は「そのコースの全単元がマスター済み（練習問題が全問正解）」。マスターは現在の回答（`answers`）で判定し、到達順・誤答の解き直し履歴は問わない。
- 未解放の間は、あと何単元でどの単元の得点が足りないかを一覧表示する。ロック理由を隠さない。
- 出題はそのコースの全問からランダムな順で1回だけシャッフルする。合格ラインは正答率80%（`Math.ceil(total * 0.8)`）。
- 結果は `state.finalChecks[courseId]` に `bestScore` `lastScore` `cleared` `bestTotal` を保存する。一度CLEARすれば以後も保持するが、単元・問題の増減で合計問題数が変わったら`cleared`を無効化し、挑戦し直しを促す。
- 修了テストの解答は `state.answers`（各論の練習問題の記録）にも `state.review`（下記の復習記録）にも一切書き込まない。単元練習の進捗や間隔反復とは独立した「認定テスト」として扱う。
- 構成リストの「修了テスト」項目はCLEAR済みなら済み表示にする。他の項目と同様、未解放でもクリックでき、ロック画面が理由を説明する。

## 今日の復習（間隔反復）

- 対象は単元ではなく**問題単位**。各問題は content.js の `id` で識別する。単元を増やす・問題を追加するときは、その単元の他の問題と衝突しない明示的な `id`（例 `<lessonId>-qN`）を付ける。一度公開した `id` は変更しない。
- 間隔は Leitner 式で `1 → 3 → 7 → 14日`。正解のたびに次の段へ進み、不正解で最初の段（即再出題）に戻る。
- 復習期限の判定は**その日の0時境界**で行う（回答時刻からの厳密な経過時間ではない）。日をまたげば「期限到来」として扱う。
- 復習の記録は、通常の練習問題に解答した瞬間（初回・retry・retry-wrongを問わない）と、復習セッション内での解答の両方で更新する。修了テストの解答では更新しない。
- 復習の対象は「練習問題を全問回答済みの単元」の問題に限る。未着手・途中の単元の問題は対象にならない。コースをまたいで1つのプールに集約する（コースごとに分離しない）。
- 1回のセッションは最大10問。誤答回数（`wrongCount`）が多い問題を優先して出題する（シャッフル後に安定ソート）。
- 本文の後ろ（構成リストと同じ場所）に「今日の復習：n問」のバナーを常設し、**復習対象（練習問題を全問回答済みの単元の問題）が0件のときはバナー全体を表示しない**。復習セッション中も非表示にする。ヘッダーには置かない（ヘッダーは要約表示に絞るため）。どの画面からでも一段スクロールすれば復習を開始できる。
- 復習セッションは `state.stage`（現在のコース・単元位置）を変更しない別レイヤーとして描画する。開始前にいた画面はセッション終了後にそのまま復元する。
- バナー内は2段構成にする。①「今日の復習：n問」＋開始ボタンの行は、今日やる分（未実施・要再確認・期限到来の合計）が0件のときだけこの行だけを隠す（バナー自体は復習対象がある限り表示し続ける）。②折りたたみ式の「状態・復習間隔別内訳」（eiken-q1-practiceの語句復習内訳と同じ区分、既定は閉）。復習対象を「未実施・要再確認・1日後・3日後・7日後・14日後」の6区分（LEITNER_LADDERの各間隔から生成）に分類して件数を出し、今日やる分が0件でも表示して進捗の見通しを確認できるようにする。
- セッション完了画面は、セッション後もなお期限到来の問題が残っていれば残数を示し「続けて復習する」で次のセッション（最大10問）へ直接進めるようにする。残数が0なら「今日の復習はすべて終わりました」に切り替え、進行ボタンは「学習に戻る」のみにする。バナーの合計件数は1セッション（最大10問）で消化しきれないことがあるため、完了画面で残数を明示しないと進捗が伝わらない。

## 保存データ

- 保存キー（`englishGrammarLearning.v3`）はこの機能追加で変更しない。`review` `reviewSession` `finalChecks` `finalRun` は新規の追加フィールドとして扱い、既存の保存データはそのまま読み込める。

## 生徒別進捗（クラウド同期）

- 共有URL（`?s=<id>&t=<token>`）があり `config.json` が揃うときだけ有効にする。無ければ完全にLocalStorageのみの匿名動作のままにする（`vendor/harness/cloud.js` は無回帰を前提に設計されている）。
- クラウドから届いた進捗は、そのまま `state` に差し替えない。`defaultState` で欠損フィールドを埋めたうえで、通常のロード直後と同じ正規化（`clampStage` → `backfillVisitedLessons` → `sanitizePersistedSessions` → `syncContentVersions`）を通す。これを省くと、今回追加した `review` / `finalChecks` 等を持たない古いクラウド保存データを読み込んだときに未定義参照でクラッシュする。
- クラウドに保存履歴が無い（新規発行の生徒URL）場合、`app_load_progress` は空オブジェクトを返す。この場合は端末側の状態をそのまま保持し、`state` を差し替えない。差し替えてしまうと、匿名時代に端末へ貯めていた進捗を空状態で上書きしてしまう。クラウドへの反映は次の `saveState()`（＝生徒の次の操作）を待つ。
- ただし「端末側を保持」してよいのは、その端末に残っている進捗が今アクセスしている生徒自身のものである場合に限る。同じ端末を複数の生徒が共有URLで使う運用（本ファイル記載）のもとでは、直前に別の生徒として同期した進捗が端末に残ったまま次の生徒の空クラウドに乗り、生徒間で進捗が混線しうる。これを防ぐため、クラウド同期が有効になった生徒IDを `<storageKey>.cloudStudent` に記録し、空クラウド＋別生徒IDのスタンプが残っている場合は端末側を保持せず、通常のロード直後と同じ正規化（版チェックを含む）を通した既定状態から始める。版チェックを省くと、version付きの単元で `state.versions` が未初期化のまま残り、次回起動時に「内容が更新された」と誤判定してその単元の回答を消してしまう。
- 共有URLの認証（`app_auth_student`）は通信失敗時に匿名扱いへフォールバックする（`cloud.js` の設計）。その間の学習はスタンプが更新されないため、後で本来の生徒として認証が成功したときにスタンプ不一致と判定され、既定状態からやり直しになる場合がある（データが混線するより安全な向きの妥協として許容する）。
- クラウド保存のトリガーは `saveState()` の1箇所にまとめる。呼び出し側（各アクションハンドラ）はクラウドの有無を意識しない。
- 進捗保存の表示（ヘッダーの `#save-status`）は、共有URLで有効な間は harness の `onStatus` が渡す文言（保存中・保存済み・失敗）にその場で置き換える。共有URLが無い匿名時は「端末に自動保存済み」のまま変更しない。
- 端末内のLocalStorageは生徒ごとに分離しない（生徒プロファイル切替UIは持たない）。同じ端末を複数の生徒で使う場合は、各生徒の共有URLでアクセスする運用を前提にする。

## 教材データ

- 教材本文と問題は `content.js`、画面遷移と採点は `app.js` に分ける。
- 教材は `curriculum.courses` のカテゴリごとに「ID・名称・概論・単元」を持つ。
- 単元を増やすときは、対象カテゴリの `lessons` に「ID・名称・本文・問題」を1件追加する。
- 各単元は「各論」の下に置き、その単元の直下へ「練習問題」を自動的に追加する。
- 単元IDは保存済み回答の識別に使うため、公開後は変更しない。
- カテゴリIDも保存済みの選択状態に使うため、公開後は変更しない。
- 各論本文に複数の`<h3>`相当の節がある場合は、`<details class="section"><summary>節見出し</summary>…</details>`で折りたたむ。最初の節は`open`にし、以降は既定で閉じる。単一の補足節だけがある場合は、その節のみを閉じた状態で追加する。

## 視覚ルール

- 配色・書体トークンは`english-level-check`・`english-grammar-trainer-v2`と共通の「Claude」テーマ（parchment / ink / clay）に揃える。地色は生成り（`--parchment #faf9f6`）、カード・本文面は`--paper #fffefa`、本文色は黒に近い濃グレー（`--ink #141413`）。書体は本文`--sans`（Segoe UI）、見出し`--serif`（Georgia）、ラベル・数値`--mono`（Cascadia Mono）。
- 主要操作は`--ink`地の黒ボタン、強調・フォーカスリングはテラコッタ（`--clay #b5532e`）、正解は緑（`--ok`）、不正解は赤（`--ng`）に使い、記号と文章も併記する。角丸は使わず、罫線（1px `--ink`／`--line`）で区切る。
- シェル全体の最大幅は1240px。サイドバー268px＋本文カード（`#content`）の実測幅は680pxを上限とし、行長を抑える。広い画面ではサイドバーの常時可視性と余白に幅を使い、行長そのものは広げない。
- ヘッダー・サイドバーの各パネル・本文カードは同じ「`--paper`地・`--ink`罫線」の見た目にそろえる。
- 見出しは短く構造を示し、本文は16px・行間1.75・おおむね30〜40emを保つ。
- 操作領域は44px以上。キーボードのフォーカスを明示する。
- 主CTAは1画面に1つとし、補助操作は`--paper`地の副ボタンにする。
- 画面幅1000px以下ではサイドバーが本文の上に積み、640px以下では構成リストを「学習の構成を見る」に折りたたみ、操作ボタンを縦に並べる（決定系グループ内では主CTAを先頭にする）。
- 折りたたみは `<details>` の開閉に委ね、閉じた `<details>` の中身をCSSで表示させない（現行ブラウザでは無効で、要素が開けなくなる）。
- 画面更新時は本文へフォーカスを移しても、利用者のスクロール位置を勝手に変えない。
